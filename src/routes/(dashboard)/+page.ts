import type { PageLoad } from '../$types';

export const load: PageLoad = (/*{ params }*/) => {
	return {
			title: '',
			description: 'A 24/7 live view of the current Ture Hounds litter; puppies of Disa and Yoshi.',
	};
};
